metadata:
  namespace: default
  type: MachineClasses.omni.sidero.dev
  id: worker
spec:
  autoprovision:
    providerid: vsphere
    providerdata: |
      cpu: 4
      memory: 16384 # in MB
      disk_size: 10 # in GB
      network: "VLAN-10"
      datastore: "atlas"
      datacenter: "UCLAB-8"
      resource_pool: "Resources"
      template: "worker-talos-omni-1-13-8"
      firmware: "efi"
